/// KomgaSync — sync engine.
///
/// Four pipelines: BootstrapSync, ReconciliationSync, EventDrivenSync,
/// MutationUploadSync. Full behavior spec in `docs/sync-engine.md`
/// and `specs/contracts/`.
public enum KomgaSync {
    public static let status = "scaffold"
}
